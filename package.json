{
  "name": "redis-client",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "functions-framework --target=index",
    "start-dev": "npx @google-cloud/functions-framework --target=summit-status --source=dist/index.js --verbose",
    "prestart": "npm run build",
    "gcp-build": "npm run build",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.2.1",
    "@google-cloud/logging": "^10.5.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "redis": "^4.6.7"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.1.6"
  }
}
